# ESP-IDF build output
build/
sdkconfig
sdkconfig.old

# Local dev overrides (e.g. real WiFi credentials) -- never commit these
sdkconfig.local

# Host-based test binary (see test/test_boot_logic.c build command)
/test_boot_logic

# Editor/OS cruft
.vscode/
.DS_Store
